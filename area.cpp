#include <iostream>

using namespace std;

int main(){

    int area,length,breadth;
    cout<<"enter the value of length and breadth";
    cin>>length>>breadth;
    area=length*breadth;
    cout<<"area"<<area;

    return 0;
}